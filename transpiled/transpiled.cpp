#include <stdio.h>

int main(void)
{
    /* list */
    {
        int i;
        i = (0);
        while ((i) < (10))
        {
            printf("%d\n", (i));
            i = ((i) + (1));
        }
    }
}

