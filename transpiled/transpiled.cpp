// this C++ code is transpiled from Pinecone
// Pinecone v0.3.0 was used

#include <stdio.h>


int getIntInput();

int getIntInput()
{
    int tmp;
    scanf("%d", &tmp);
    return tmp;
}

int main()
{
    // list
    {
        if (true)
        {
            1;
        }
        else
        {
            2;
        }
        int a;
        a = (getIntInput());
        int b;
        b = (getIntInput());
        printf("%d\n", ((a) % (b)));
    }
}

